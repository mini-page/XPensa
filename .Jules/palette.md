## 26-03-2026 - [Add semantics to QuickActionBar] **Learning:** [Custom action bar items without explicit button roles are not properly announced by TalkBack/VoiceOver] **Action:** [Always wrap interactive components like InkWell in Semantics(button: true) and provide an accurate label when using non-standard widgets like custom action items]
## 30-03-2026 - [Add semantics to TransactionCard] **Learning:** [Core repeated list items wrapped in InkWell need explicit Semantics to be read cleanly as a single interactive element rather than distinct pieces, and icon-only popup menus must have tooltips] **Action:** [Wrap InkWell in Semantics(button: true, label: '[Description]') to narrate item content correctly and provide tooltips to PopupMenuButton]

## 26-03-2026 - [Add semantics to GridCategoryCard and AddCategoryCard] **Learning:** [Custom grid cards and action cards built with InkWell are not properly announced by screen readers as interactive buttons, reducing accessibility] **Action:** [Always wrap custom interactive cards (like InkWell without a built-in semantic role) in Semantics(button: true) and provide descriptive labels and Tooltips for clear intent]
## 29-05-2024 - [Add semantics and tooltips to icon-only buttons] **Learning:** [Custom icon-only buttons lack screen reader context and visual clarity on hover] **Action:** [Wrap interactive components like InkWell in Semantics(button: true, label: tooltip) and Tooltip(message: tooltip) when creating custom icon-only buttons]

## 01-04-2026 - [Semantics for Custom Buttons] **Learning:** GestureDetector and InkWell do not inherently provide button roles or context to screen readers, making them difficult to use for accessibility. **Action:** Always wrap interactive custom components (like InkWell/GestureDetector) with Semantics(button: true) and provide descriptive labels.
## 04-04-2026 - [Accessibility] Semantics and Tooltip for Custom Icon Buttons **Learning:** Custom interactive widgets built using primitives like `Material` and `InkWell` around an `Icon` lack implicit accessibility and tooltips, leaving them unannounced to screen readers and visually ambiguous. **Action:** Always wrap custom icon-only actions with `Semantics(button: true, label: tooltip)` and `Tooltip(message: tooltip)`.
## 12-05-2024 - [Add semantics to icon-only buttons] **Learning:** [In Flutter, setting the  property on an  inherently provides accessibility semantics for TalkBack/VoiceOver; there is no need to redundantly wrap the  in a  widget.] **Action:** [Provide the  argument for  instead of nesting it in ]
## 12-05-2024 - [Add semantics to icon-only buttons] **Learning:** [In Flutter, setting the `tooltip` property on an `IconButton` inherently provides accessibility semantics for TalkBack/VoiceOver; there is no need to redundantly wrap the `IconButton` in a `Semantics` widget.] **Action:** [Provide the `tooltip` argument for `IconButton` instead of nesting it in `Semantics(button: true)`]
